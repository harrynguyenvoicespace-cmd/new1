Frontend code is grouped by route-facing page.

- `pages/<page-name>` contains the screen shell for one visible page.
- `shared` contains frontend-only UI used by more than one page.
- Domain editor modules that are reused deeply still live in `src/features`.
